<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/artist"
        android:icon="@drawable/ic_artista"
        android:title="Artistas"
        app:showAsAction="never"
        android:enabled="true"
        />
    <item android:id="@+id/album"
        android:icon="@drawable/ic_album"
        android:title="Album"
        app:showAsAction="never"
        android:enabled="true"
        />
    <item android:id="@+id/cancion"
        android:icon="@drawable/ic_song"
        android:title="Cancion"
        app:showAsAction="never"
        android:enabled="true"
        />
    <item android:id="@+id/playlist"
        android:icon="@drawable/ic_list"
        android:title="Playlist"
        app:showAsAction="never"
        android:enabled="true"
        />

</menu>